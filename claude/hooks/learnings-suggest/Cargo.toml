[package]
name = "learnings-suggest"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1"
aho-corasick = "1"

[profile.release]
opt-level = 3
lto = "fat"
strip = true
codegen-units = 1
panic = "abort"
